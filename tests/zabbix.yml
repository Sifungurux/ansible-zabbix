---
- hosts: PRODUCTION
  become: yes
  become_method: sudo
  gather_facts: yes
  roles:
          - {role: ansible-zabbix}
          - {role: ansible-mysql, mysql_db: [{name: zabbix}], mysql_users: [{name: "{{ zabbix_user }}", pass: "{{zabbix_pass }}", priv: "*.*:ALL"}]}
